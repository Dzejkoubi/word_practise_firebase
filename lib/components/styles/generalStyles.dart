import 'package:flutter/material.dart';

class AppShadows {
  static final customBoxShadow = [
    BoxShadow(
      color: Colors.grey.withOpacity(0.5),
      spreadRadius: 1,
      blurRadius: 10,
      offset: Offset(3, 3),
    ),
  ];
}
