import 'package:flutter/material.dart';

class wordLists extends StatelessWidget {
  const wordLists({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
